const rng = new Random()

export class JigsawGenerator {
    public constructor(
        private depth: number,
        private radius: number
    ) {

    }

    public generate() {
        
    }
}